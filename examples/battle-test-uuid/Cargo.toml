[package]
name = "battle-test-uuid"
version = "0.1.0"
edition = "2021"

[dependencies]
borrowscope-runtime = { path = "../../borrowscope-runtime", features = ["track"] }
borrowscope-macro = { path = "../../borrowscope-macro" }
uuid = { version = "1", features = ["v4", "v5"] }
