[package]
name = "allocator-sim"
version = "0.1.0"
edition = "2021"
description = "Memory allocator simulator demonstrating all BorrowScope runtime features"

[dependencies]
borrowscope-runtime = { path = "../../borrowscope-runtime", features = ["track"] }
