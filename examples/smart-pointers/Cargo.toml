[package]
name = "smart-pointers"
version = "0.1.0"
edition = "2021"
description = "Deep dive into Rust smart pointers with BorrowScope tracking"

[dependencies]
borrowscope-runtime = { path = "../../borrowscope-runtime", features = ["track"] }
